[project]
name = "problem-of-evil"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "flask-caching>=2.3.0",
    "flask-compress>=1.15",
]

[dependency-groups]
prod = [
    "gunicorn>=25.0.3",
]
